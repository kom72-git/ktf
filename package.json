{
  "name": "ktf",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@fancyapps/ui": "^6.0.34",
    "react-router-dom": "^7.9.3"
  },
  "scripts": {
    "install-all": "cd react-app && npm install && cd ../server && npm install",
    "build": "cd react-app && npm run build",
    "start": "cd server && npm start"
  }
}
